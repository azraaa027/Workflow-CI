name: titanic-skilled

# Tidak perlu conda_env jika pakai --env-manager=local
# conda_env: conda.yaml ← hapus baris ini jika tidak pakai conda

entry_points:
  train:
    command: "python modelling.py"